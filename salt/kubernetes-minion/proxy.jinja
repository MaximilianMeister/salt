###
# kubernetes proxy config

# default config should be adequate

# Add your own!
KUBE_PROXY_ARGS="\
    --kubeconfig={{ pillar['paths']['var_kubelet'] }}/kubeconfig \
    --proxy-mode=iptables \
    --v=2"
